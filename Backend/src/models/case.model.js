/**
 * Case records are derived from AI PredictionResponse.case_id (future persistence).
 * @typedef {Object} CaseModel
 * @property {string} case_id
 * @property {string} primary_diagnosis
 * @property {number} confidence
 */

module.exports = {};
