/**
 * Mirrors AI FeedbackRequest / FeedbackResponse schemas.
 * @typedef {Object} FeedbackRequestModel
 * @property {string} case_id
 * @property {boolean} correct
 * @property {number} quality_rating
 * @property {string|null} [comment]
 * @property {boolean} [escalate]
 * @property {string} clinician_id
 */

module.exports = {};
