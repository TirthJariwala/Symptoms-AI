/** Email integration placeholder — wire SendGrid/SES when notifications are required. */
const sendEmail = async () => {
  throw new Error("Mailer not configured.");
};

module.exports = { sendEmail };
